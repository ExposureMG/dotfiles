yt-dlp
update-grub
cd ~/dotfiles
git commit -m "update"
git add *
git commit -m "update"
git push
yt-dlp
export $PATH
sudo mv ~/.local/bin/* /usr/local/bin/
git config --global user.name
dbus-send --session --print-reply --dest=org.freedesktop.DBus / \\
    org.freedesktop.DBus.GetConnectionUnixProcessID \\
    string:org.freedesktop.secrets
kwalletmanager5
ssh
ssh-keygen --help
openssh
pacup openssh
ssh-keygen --help
man ssh-keygen
ssh-keygen
lsblk
sudo btrfs subvol create /mnt/virt/images
sudo btrfs subvol create /mnt/virt/backup
sudo btrfs subvol create /mnt/virt/cloud
